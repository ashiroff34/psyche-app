# Content Radar — 2026-09-14

Automated daily sweep for psychology accuracy risks and SEO coverage. No source code was changed.

## Verdict

**No new issues, and every open item from 2026-09-13 is still open.** No commits since 09-13 touched `src/data`, `src/app/enneagram`, `src/app/compatibility`, `src/app/personality`, or `src/app/cognitive-functions`.

---

## 1. Passion / fixation consistency: 1 open error

- The structured `passion` fields for all 9 types match Ichazo: Anger, Pride, Deceit, Envy, Avarice, Fear, Gluttony, Lust, Sloth.
- **Still open:** [src/data/enneagram.ts:134](src/data/enneagram.ts:134) says "Resentment (Ichazo's passion for the One)". Resentment is Type 1's **fixation**. The passion is Anger, and the field at line 102 gets this right. Suggested fix: "Anger (Ichazo's passion for the One), crystallized as resentment, builds when..."
- Low severity, unchanged: the Type 3, 6, 8, and 9 prose glosses mix passion and fixation terms ("rendered as Vanity", "Fear/Cowardice", "Lust/Excess", "Sloth/Indolence"). They are labeled as alternate renderings, so they are not counted as errors.
- The `fixation` fields describe each pattern without naming the fixation term. Only Type 3 names its term ("Vanity"). Nothing is misnamed; this is just inconsistent.

## 2. Countertype designations: PASS

The flags are `sx/1, sp/2, sp/3, sp/4, sx/5, sx/6, so/7, sx/8, so/9`. That is one per type and matches Naranjo and `project_naranjo_countertypes.md`. The spec's `sp/1` is still a false positive in the spec itself; Naranjo's countertype is SX-1, "Zeal".

## 3. Tritype names: still open (no change since 09-10)

| Location | Current | Fauvre | Status |
|---|---|---|---|
| `tritypes` 135 | The Strategist | The Technical Expert | open, and duplicates 258 |
| `tritypes` 259 | The Peacekeeper | The Problem Solver | open |
| [tritypes.ts:196](src/data/tritypes.ts:196) 479 | The Gentle Spirit | The Free Spirit | open |
| [tritypes.ts:61](src/data/tritypes.ts:61) `"593"` | The Problem Solver | 359 = The Thinker | open |
| [tritypes.ts:54](src/data/tritypes.ts:54) `"514"` | The Technical Expert | 145 = The Researcher | open |
| [tritypes.ts:42](src/data/tritypes.ts:42) `"126"` | The Mentor | 126 = The Supporter | open |

The spec's banned-name list gets matches ("The Researcher" on 145, "The Problem Solver" on ordered 593). Both are real Fauvre names. 145 is correct; 593 has the name on the wrong code, which the table above already covers.

## 4. SEO page count: ~389 / 1,000 (38.9%)

| Surface | Pages |
|---|---|
| Enneagram static `page.tsx` | 119 |
| Subtypes `/enneagram/subtypes/[slug]` | 27 |
| Tritypes `/enneagram/tritype/[code]` | 27 |
| Enneagram app route `/enneagram/[type]` | 9 |
| Cognitive functions | 16 |
| Compatibility (36 pairs + 9 same-type + index) | 46 |
| MBTI x Enneagram crossover `/personality/[slug]` | 144 |
| Personality index | 1 |
| **Total** | **~389** |

The spec's literal commands return 9 + 16 + 46 + 1 = 72. Those commands miss routes generated by `generateStaticParams`. Gap: 611. No change from 09-13.

## 5. Compatibility gaps: none

All 36 unique pairs and all 9 same-type pairs exist. The only pair not in ascending order is 1+9, which lives at `enneagram-9/enneagram-1`.

---

## Actions for the founder (unchanged)

1. Fix [enneagram.ts:134](src/data/enneagram.ts:134): Resentment is Type 1's fixation, not its passion. Send to blinky.
2. Fix the tritype names that sit on the wrong codes (135, 259, 479, and ordered 593/514/126). Send to blinky.
3. Correct the radar spec: `sp/1` → `sx/1`, and replace the banned-name list with a "Fauvre name on wrong code" check.
4. Normalize the 1+9 compatibility route.

This run was unattended, so the interactive "Fix now / Send to blinky / Skip" prompt could not be shown. Nothing was changed.
