# Content Radar — 2026-09-13

Automated daily sweep for psychology accuracy risks and SEO coverage. No source code was changed.

## Verdict

**One new accuracy error found** (Type 1 passion misattributed in prose). Tritype and radar-spec items from 2026-09-08 to 09-11 are still open. The content data files have had no commits since 2026-09-11 (10f8d30 only touched `learn/page.tsx` and `journal/page.tsx`).

---

## 1. Passion / fixation consistency: 1 NEW ERROR

The `passion` and `fixation` fields for all 9 types are still correctly attributed (same result as 09-11).

**New finding:** the Type 1 `fullDescription` prose gets it wrong at [src/data/enneagram.ts:134](src/data/enneagram.ts:134):

> "Resentment (Ichazo's passion for the One) builds when..."

In Ichazo, Resentment is the **fixation** for Type 1 and Anger is the **passion**. The structured `passion` field at line 102 has this right ("Anger (Ichazo)"), so the page contradicts itself. Earlier radars checked the fields and missed this line. No other `<fixation term> (Ichazo's passion...)` misattribution exists anywhere in `src/`.

Suggested fix (send to blinky): "Anger (Ichazo's passion for the One), crystallized as resentment, builds when..."

Style note carried over from before (low severity): the Type 3, 6, 8, and 9 prose glosses mix passion and fixation terms ("Deceit… rendered as Vanity", "Fear/Cowardice", "Lust/Excess", "Sloth/Indolence"). These are labeled as alternate renderings, so they are not errors.

## 2. Countertype designations: PASS

The flags are `sx/1, sp/2, sp/3, sp/4, sx/5, sx/6, so/7, sx/8, so/9`. That is one per type and matches Naranjo and `project_naranjo_countertypes.md`.

The radar spec still lists **sp/1**, which is wrong (the SX-1 is Naranjo's countertype, "Zeal"). Every run reports this as a false positive. The spec file was not edited because it contains the radar's own instructions.

## 3. Tritype names: still open (no change since 09-10)

| Location | Current | Fauvre | Status |
|---|---|---|---|
| `tritypes` 135 | The Strategist | The Technical Expert | open, and duplicates the 258 H1 |
| `tritypes` 259 | The Peacekeeper | The Problem Solver | open |
| `tritypes` 479 | The Gentle Spirit | The Free Spirit | open |
| [tritypes.ts:61](src/data/tritypes.ts:61) `"593"` | The Problem Solver | 359 = The Thinker | open. The name belongs to 259, and it conflicts with the array's "The Diplomatic Achiever" |
| [tritypes.ts:54](src/data/tritypes.ts:54) `"514"` | The Technical Expert | 145 = The Researcher | **newly noted.** The name belongs to 135 |
| [tritypes.ts:42](src/data/tritypes.ts:42) `"126"` | The Mentor | 126 = The Supporter | **newly noted.** It conflicts with the array entry |

"The Researcher" (145) is still present and is Fauvre-correct. The spec's banned-name list ("The Researcher", "The Problem Solver", "The Free Spirit") names three real Fauvre archetypes. The real problem is that those names sit on the wrong codes, not that they exist.

The 27 permuted duplicate codes (55 array entries for 28 type sets) flagged on 09-10 are also still unresolved.

## 4. SEO page count: ~389 / 1,000 (38.9%)

| Surface | Pages |
|---|---|
| Enneagram static `page.tsx` (type pages, wings, hubs, relationships, work, growth, etc.) | 119 |
| Subtypes `/enneagram/subtypes/[slug]` | 27 |
| Tritypes `/enneagram/tritype/[code]` | 27 canonical |
| Enneagram app route `/enneagram/[type]` | 9 |
| Cognitive functions | 16 |
| Compatibility (36 pairs + 9 same-type + index) | 46 |
| MBTI x Enneagram crossover `/personality/[slug]` | 144 |
| Personality index | 1 |
| **Total** | **~389** |

The spec's literal commands return 9 + 16 + 46 + 1 = 72. Those commands miss the routes generated by `generateStaticParams`. No change from 09-11. Gap: 611.

## 5. Compatibility gaps: none

All 36 unique pairs and all 9 same-type pairs exist. 1+9 still lives at `enneagram-9/enneagram-1`, the only pair not in ascending order.

---

## Actions for the founder

1. **Fix [enneagram.ts:134](src/data/enneagram.ts:134)**: Resentment is Type 1's fixation, not its passion. This is new today; send to blinky.
2. Fix the tritype names on the wrong codes (135, 259, 479, and ordered 593/514/126). Send to blinky.
3. Correct the radar spec: `sp/1` → `sx/1`, and replace the banned-name list with a "Fauvre name on wrong code" check.
4. Normalize the 1+9 compatibility route.

Note: this run was unattended, so the interactive "Fix now / Send to blinky / Skip" prompt could not be shown. Nothing was changed.
